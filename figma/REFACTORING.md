# Рефакторинг проекта

## Новая структура проекта

### 📁 constants/
Содержит все константы проекта:
- `projects.ts` - константы проектов (изображения, названия, логотипы)

### 📁 types/
Общие TypeScript типы:
- `index.ts` - интерфейсы для компонентов

### 📁 hooks/
Кастомные React хуки:
- `useIntersectionObserver.ts` - отслеживание видимости элементов
- `useScrollArrows.ts` - управление стрелками навигации при скролле

### 📁 config/
Конфигурационные файлы:
- `animations.ts` - настройки анимаций и готовые варианты

### 📁 utils/
Утилиты:
- `smoothScroll.ts` - плавная прокрутка

## Улучшения

### ✅ Вынесены константы
Все константы проектов (изображения, названия, логотипы) вынесены в `constants/projects.ts`

### ✅ Созданы кастомные хуки
- `useIntersectionObserver` - заменяет повторяющийся код IntersectionObserver
- `useScrollArrows` - управление стрелками навигации

### ✅ Унифицированы анимации
Стандартные варианты анимаций в `config/animations.ts`:
- `fadeInUp` - появление снизу вверх
- `fadeInLeft` - появление слева
- `fadeInRight` - появление справа
- `scaleIn` - масштабирование

### ✅ Улучшена типизация
Созданы общие типы в `types/index.ts`

## Использование

### Импорт констант
```typescript
import { PROJECT_NAMES, PROJECT_IMAGES, PROJECT_PREVIEWS } from '../constants/projects';
```

### Использование хуков
```typescript
import { useIntersectionObserver } from '../hooks/useIntersectionObserver';

const isVisible = useIntersectionObserver(ref, { threshold: 0.3 });
```

### Использование анимаций
```typescript
import { fadeInUp } from '../config/animations';

<motion.div {...fadeInUp} animate={isVisible ? fadeInUp.animate : fadeInUp.initial} />
```

## Следующие шаги

1. ✅ Вынести константы проектов
2. ✅ Создать кастомные хуки
3. ✅ Унифицировать анимации
4. ⏳ Оптимизировать остальные компоненты (About, HomeOffice, Footer)
5. ⏳ Создать общие компоненты для повторяющихся элементов
